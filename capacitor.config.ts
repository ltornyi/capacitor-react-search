import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ltornyi.capacitorReactSearch',
  appName: 'capacitor-react-search',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
